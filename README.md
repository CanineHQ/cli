# Canine CLI

This is the open source project for the Canine CLI.

The Canine CLI allows you to create shells into running projects, trigger cron jobs and more.

### Authentication
Before actions can be taken, login with:

`k9 auth login`

Or, set the `CANINE_API_TOKEN` in your environment variable.

Check your current authentication status with

`k9 auth status`

